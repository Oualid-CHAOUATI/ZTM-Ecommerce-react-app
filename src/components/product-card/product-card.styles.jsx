import styled from "styled-components";
export const ProductCard_styled = styled.div`

border:2px solid red;

  .img-wrapper {
    height: 300px;
    aspect-ratio: 1;

    img {
      height: 100%;
      width: 100%;
      object-fit: cover;
    }
  }
}
`;

// ? existing element ?
// ? const btnBlue = styled(normalBtn) //! extends
// ? Add other styles

// .product-card {
